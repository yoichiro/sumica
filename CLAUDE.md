# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Sumica AI Studio chains a local LLM (LM Studio) and Stable Diffusion (AUTOMATIC1111/Forge) to turn natural-language (e.g. Japanese) prompts into images. When the user is signed in via Google (Firebase Auth), the client persists images to Firebase Storage and metadata to Firestore; when signed out, the server saves locally. See `README.md` for the full LM Studio / Stable Diffusion setup prerequisites.

## Commands

Run from the **repository root** unless noted:

```bash
npm install        # installs root, client, and server deps in one shot
npm run dev        # concurrently runs server (tsx watch) + client (vite)
npm run dev:server # server only — http://localhost:5000
npm run dev:client # client only — http://localhost:5173
```

- **Lint** (client): `npm run lint --prefix client` (uses **oxlint**, not ESLint; config in `client/.oxlintrc.json`).
- **Type-check / build** (client): `npm run build --prefix client` (`tsc -b && vite build`).
- **Type-check** (server): `npm run typecheck --prefix server` (`tsc --noEmit`). The server runs `.ts` directly via **tsx** — there is no build step and no `dist/`; `tsc` is type-checking only.
- **Tests** (client): `npm run test:run --prefix client` for a one-shot Vitest run, or `npm run test --prefix client` for watch mode. Vitest config lives in `client/vite.config.ts` (jsdom env, `client/src/test-setup.ts` for `@testing-library/jest-dom`). Current coverage: `components/presets.test.ts` (aspect-ratio resolver/finder round-trips for both SDXL and SD1.5), `components/loadIntoFormState.test.ts` (pure state transition triggered by "フォームにロード", covering same-arch and cross-arch loads plus fallback heuristics), `components/RankingPanel.test.tsx` (Top-N ordering by absolute favs, arch-chip resolution, empty state, apply-to-form callback), `components/GenerationBadge.test.tsx` (spinner badge visibility across genStatus + batchProgress combinations), `components/captionFields.test.ts` (gallery caption / info-panel field composition from GenerationData), `components/lightboxKeyboard.test.ts` (pure key → action resolver used by the lightbox effect), `components/rangeSelection.test.ts` (Shift-click range-select set math), `utils/rankingAnalysis.test.ts` + `utils/rankingRollup.test.ts` (the ranking pipeline's rollup normalization and sort), and `i18n/index.test.ts` (locale resolution from `?hl=` query + `navigator.language`). Server has no tests yet.

## Architecture

Two packages, each collapsed into a small number of source files:
- **`server/index.ts`** — Express 5 (TypeScript, ESM) API, all routes + the LLM/SD pipeline. Firebase-free at runtime; run on the fly with tsx. Uses `sharp` to produce a 256px WebP thumbnail alongside each locally-saved PNG.
- **`client/src/App.tsx`** — orchestrator: all state, useEffects, generation pipeline, and top-level composition (~1470 lines).
- **`client/src/components/`** — extracted UI components, flat layout (see ADR 15). `AppHeader`, `ControlPanel` (the left-column form), `PreviewPanel`, `HistoryGallery`, `Lightbox`, `BatchGenerationModal`, `DeleteConfirmModal`, `ToastContainer`. Plus `presets.ts` with `SDXL_PRESETS`/`SD15_PRESETS`/resolver+finder helpers shared between App and BatchGenerationModal. All state lives in App.tsx; components receive props (hybrid strategy: prop drilling by default, Context reserved for future need).
- **`client/src/firebase.ts`** — Firebase SDK initialization (Auth, Firestore, Storage) and helper types (`AuthUser`, `GenerationRecord`, `GenerationParams`).

### The generation pipeline (the key flow)

The enhance and generate steps are **split into two client requests on purpose** so the UI can render a live step indicator (`loadingStep` 1→2→3). Editing one side without the other will break this:

1. Client `POST /api/enhance` → server `enhancePrompt()` asks LM Studio (`/v1/chat/completions`, OpenAI-compatible) to translate+expand the prompt. The LLM is instructed to reply with `<prompts><positive>…</positive><negative>…</negative></prompts>`, which the server **parses by regex**. If the model omits the tags, it falls back to the raw prompt / a default negative. The system prompt also teaches the LLM to convert **natural-language emphasis cues** into SD's `(phrase:weight)` syntax across three tiers plus a suppress bucket — Mild → `1.2` (JP: かなり / 特に / とても / しっかり / ちゃんと / 多少, EN: especially / very / quite / notably), Strong → `1.3` (JP: 強く / めっちゃ / すごく / はっきり / 大きく / しっかりと, EN: strongly / clearly / pronounced / significantly), Extreme → `1.4` (JP: ものすごく / 極めて / 際立って / 目立たせて / 強調して / とびっきり / 完全に / 一切, EN: highlight / emphasize / prominent / extremely / utterly / completely), and Suppress → placed in the negative prompt at `0.8` (JP: あまり〜させないで / 控えめに / 少なめに / 避けて, EN: subtle / less). Multiple cues on the same concept collapse to the strongest tier, and generic quality tags like "masterpiece" / "best quality" are deliberately never wrapped. Users write natural language only and cannot edit the expanded prompt directly, so this LLM-side inference is the only path from natural cues to real SD emphasis; the authoritative cue list lives in `server/index.ts:143-186` and should be updated there first when adding new cues.
2. Client `POST /api/generate` with the returned positive/negative, **`skipEnhance: true`**, and **`clientPersist: true`** (when the user is signed in). `generateImage()` then calls Stable Diffusion `/sdapi/v1/txt2img` (600s timeout — Hires.fix's second pass can take well beyond the old 180s cap) and gets a base64 PNG. An optional `model` in the request is passed as `override_settings.sd_model_checkpoint` (SD switches checkpoint and keeps it loaded). When `clientPersist: true`, the server returns `{ success: true, image: <base64>, params: {...} }` without saving anything; when `clientPersist` is absent/false, it local-saves and returns `{ success: true, data: metadata }`. If the user cancels the generation via the Cancel button before it finishes, `/api/generate` instead returns `{ success: false, cancelled: true }` and persists nothing, regardless of `clientPersist`.
3. Client receives the response. If signed in (`clientPersist` path): client uploads the base64 image to Firebase Storage (`users/{uid}/images/…`) and writes metadata to Firestore (`users/{uid}/generations/{id}`). If signed out (local path): server has already saved; client refreshes history via `/api/history`.

### SDXL preset picker (main-form resolution UI)

For **SDXL** the main form exposes a **aspect ratio + orientation + size** picker instead of free width/height dropdowns. The `SDXL_PRESETS` constant in `client/src/App.tsx` enumerates the 7 aspect ratios (`1:1`, `4:3`, `9:7`, `3:2`, `16:9`, `21:9`, `3:1`), each with three sizes (`S` ≈ 0.5–0.7MP, `M` ≈ 1MP SDXL-native ⭐, `L` ≈ 1.3–1.5MP) and both landscape/portrait dimensions. The picker sets the same underlying `width` / `height` state the rest of the app already uses — nothing downstream (generation, persistence, metadata) needs to change. A ⭐ badge on a ratio chip (`ratioIsBucket`) marks ratios SDXL was trained on directly (`1:1`, `9:7`, `3:1`); a ⭐ on a size chip (`isSdxlBucket`) marks the specific `W×H` as an SDXL training bucket (all `M` sizes are buckets in the default table). When the architecture toggle flips to SDXL, `findSdxlSelection(width, height)` tries to seed the picker from the current dimensions; if no preset matches (e.g. after switching from SD1.5), the app resets to `1:1 M` = `1024×1024`.

For **SD1.5** the main form uses the same picker shape via `SD15_PRESETS`: 7 ratios (`1:1`, `5:4`, `4:3`, `3:2`, `5:3`, `16:9`, `2:1`). Because SD1.5 wasn't trained with aspect-ratio buckets and quality falls off quickly outside ~0.25–0.5MP, only `1:1` exposes the S/M/L size ladder (512² / 768² / 1024²). All other SD1.5 ratios carry a single M spec — the size toggle hides itself when a non-square ratio is active. Landscape dimensions are stored on the preset; portrait is derived by swapping W↔H at resolve-time. Same `findSd15Selection(width, height)` reverse lookup seeds the picker on architecture switches and on "フォームにロード"; a non-matching pair falls back to `1:1 S` (512×512, SD1.5's native).

### Batch generation (client-side sequential loop)

"まとめて生成" opens a modal with three mutually-exclusive modes selected by a segmented tab: **count** (2–10 copies at the main-form size), **size combinations** (one image per size-space cross product; the axes depend on the active architecture — see below), and **model cycling** (one image per available SD checkpoint — `sdModels`, already XL-filtered server-side — at the current main-form size; the button is disabled when no models are loaded). The **size combinations** mode is architecture-specific:
- **SD1.5**: three-axis cross product of **aspect ratio × orientation × size** driven by `SD15_PRESETS` (see the SD1.5 preset picker section below). Same structural shape as SDXL, but the size axis only produces extra jobs for `1:1` — non-square SD1.5 ratios only have an M spec, so they emit 1 job per selected orientation regardless of which sizes are checked.
- **SDXL**: three-axis cross product of **aspect ratio × orientation × size** driven by `SDXL_PRESETS` (see the SDXL preset picker section below). 1:1 collapses orientation (one job per selected size, regardless of the landscape/portrait toggle); non-square ratios cross-product each selected orientation with each selected size. There is **no upper cap** on the emitted `BatchJob[]` — the "生成する" button disables only when the selection is empty, so the user can queue the entire cross product intentionally. All modes build a normalized job list of `{ width, height, model? }` entries (`BatchJob[]`) that `handleBatchGenerate(jobs)` (in `client/src/App.tsx`) runs through a single sequential loop — the same per-image progress indicator ("画像 i/N"), incremental persistence (Firebase when signed in, server-local when signed out), continue-on-failure, and summary toast back all modes. A job's optional `model` overrides the form's `selectedModel` for that one image (precedence `job.model ?? selectedModel`); count and size jobs omit it and so use `selectedModel` as before. The prompt is enhanced **once** before the loop; seed follows the existing seed-lock setting, identical to single generation. SD's own Batch Count parameter is deliberately **not** used. The server is unchanged — batch is purely a client-side loop over the existing single-image endpoint. The shared helpers `enhanceOnce` / `generateImage` / `persistResult` / `generateAndPersist` back both the single and batch flows; `generateImage` and `generateAndPersist` take `width`/`height` and an optional `modelOverride` as explicit parameters so each job's size and model flow through to SD and into the persisted metadata.

### Storage: client Firebase ↔ server local fallback

The runtime server is **Firebase-free** — the Express app never imports `firebase-admin`; no service-account key is required to run `npm run dev`. Storage is split by auth state:

- **Signed in** (Firebase Auth via Google): the client (`client/src/firebase.ts`) uploads the base64 image to Firebase Storage at `users/{uid}/images/<timestamp>.png` and writes metadata to Firestore at `users/{uid}/generations/{id}`. Alongside the PNG, the client generates a 256px WebP thumbnail via Canvas API (`client/src/utils/thumbnail.ts`) and uploads it to `users/{uid}/thumbs/<timestamp>.webp`; the download URL is stored on the Firestore doc as `thumbnailUrl`. Storage rules (`storage.rules`) allow reads/writes under both `users/{uid}/images/*` and `users/{uid}/thumbs/*` for the owning user. History is driven by a live Firestore `onSnapshot` subscription, **scoped to the gallery's date-filter day** via a `where('timestamp', >=/<=, dayBounds)` range query — every image from the selected day is returned with no per-day count cap. Changing the date filter tears down the old subscription and opens a new one for the new day. Same-field `where`+`orderBy` on `timestamp` uses Firestore's automatic single-field index (no composite index required). Deletion removes the Firestore doc plus both Storage objects.
- **Signed out**: the client sends `POST /api/generate` without `clientPersist`; the server saves to `server/outputs/` (PNG + `<name>_thumb.webp` sidecar produced by `sharp`) and records metadata in `server/outputs/metadata.json`, served back via `GET /api/outputs/*`. `/api/history` reads that JSON. `/api/generations/delete` removes the image files, thumbnail files, and JSON entries.

The gallery grid uses `thumbnailUrl ?? imageUrl` — legacy records without a thumbnail fall back to full-res, and the lightbox always fetches `imageUrl` at full resolution regardless.

The `clientPersist` flag in the `/api/generate` request body is the switch: present-and-true → server returns raw base64 and skips saving; absent/false → server saves locally and returns metadata.

**One-off maintenance scripts** live in `server/scripts/` and use `firebase-admin` (a **devDependency**, not loaded at runtime): `backfill-thumbnails.ts` regenerates local sidecar WebPs for pre-existing PNGs, and `backfill-firebase-thumbnails.ts` does the same across the cloud path via `collectionGroup('generations')`. The cloud script requires `server/firebase-key.json` (a downloaded service-account JSON — matches `*key.json` in `.gitignore`) and honors `FIREBASE_STORAGE_BUCKET` in `server/.env` for projects whose bucket isn't `${project_id}.appspot.com`. Both scripts are idempotent (skip records with an existing `thumbnailUrl`) and support `THUMB_DRY_RUN=1`.

### Server endpoints

`lmStudioUrl`, `stableDiffusionUrl`, and `lmStudioModel` are set once from `server/.env` at boot (as module-level `const`s); a runtime-override endpoint used to exist but was removed along with its UI dialog — change targets by editing `.env` and restarting. `GET /api/status` still exposes the current values plus `localHistoryCount`. `GET /api/health` pings both upstreams (LM Studio `/v1/models`, SD `/sdapi/v1/sd-models`) and always returns 200 with per-service `connected` flags — the client polls it every 20s to drive the top-right status badges. `GET /api/sd-models` proxies SD's `/sdapi/v1/sd-models` + `/sdapi/v1/options` to return `{ models, current }` for the checkpoint picker in the advanced-settings UI. `GET /api/sd-samplers`, `GET /api/sd-schedulers`, and `GET /api/sd-loras` likewise proxy SD's sampler, noise-scheduler, and LoRA lists for the advanced-settings pickers. The scheduler endpoint hits `/sdapi/v1/schedulers` (AUTOMATIC1111 ≥1.9 / recent Forge); older SD builds return 404 and the proxy degrades to an empty list, hiding the scheduler picker client-side. Selected LoRAs are applied by appending comma-separated `<lora:name:weight>` tags to the positive prompt at generation, and the `model`/`sampler`/`scheduler`/`seed`/`loras` choices are persisted in the generation metadata. `GET /api/sd-upscalers` proxies SD's `/sdapi/v1/upscalers` and `/sdapi/v1/latent-upscale-modes`, merging both into one flat name list, for the Hires.fix upscaler picker in the advanced-settings UI. `GET /api/sd-vaes` proxies SD's `/sdapi/v1/sd-vae` for the SDXL external-VAE picker (the client currently exposes it only when the SDXL toggle is active; SD1.5 users get the checkpoint's default VAE). `POST /api/generate` also accepts optional SDXL-only `refiner` / `refinerSwitchAt` / `vae` fields — when a refiner is set, the payload gains `refiner_checkpoint` + `refiner_switch_at`; a non-empty `vae` other than `"Automatic"` is passed via `override_settings.sd_vae`. Both are persisted into the generation metadata (and the Firestore doc when signed in) so they round-trip through the gallery's "フォームにロード" button. `GET /api/sd-progress` proxies SD's `/sdapi/v1/progress` (with `skip_current_image: true` to avoid pulling a large base64 preview image on every poll) to return `{ progress, etaRelative }`, degrading to zeros on failure like the other SD proxies; the client polls this every 1.5s during step 2 (image generation) to show elapsed/remaining time and a progress bar. `POST /api/generations/delete` (body `{ ids }`) is **local-only**: removes the image files and `metadata.json` entries from `server/outputs/`; the gallery supports single-click select / double-click open, and deletion is gated behind a confirm modal. (When signed in, deletion is handled client-side via Firebase SDK calls, not this endpoint.) `POST /api/generate/interrupt` calls SD's own `/sdapi/v1/interrupt` to stop the currently-running generation job; it's best-effort and always responds `{ success: true }`, even if the call to SD fails. It works by setting a module-level `cancelRequested` flag that the in-flight `/api/generate` handler checks once SD's (now-interrupted) response comes back, returning the `{ success: false, cancelled: true }` shape above instead of persisting the half-finished image. A single flag suffices because SD only processes one job at a time in this single-local-user tool — no per-job/job-ID tracking is needed.

### Client ↔ server wiring

`client/src/App.tsx` derives `API_BASE` from the page's own host — ``const API_BASE = `http://${window.location.hostname}:5000/api` `` — there is **no Vite proxy**; the client talks to the server directly. This deliberately tracks `window.location.hostname` instead of a hardcoded `127.0.0.1`: under **WSL2, Windows→WSL forwarding can work for `localhost` but NOT for `127.0.0.1`**, so a hardcoded `127.0.0.1` makes every API call fail from a Windows browser even though the page loaded fine — keep the host dynamic. CORS is **restricted to the frontend origin(s)** (default `http://localhost:5173,http://127.0.0.1:5173`, overridable via the `CORS_ORIGINS` env var) — not wide open — so an arbitrary website can't drive the API; both `localhost` and `127.0.0.1` origins are allowed so the dynamic host resolves either way. Note the asymmetry: **server-side** URLs (server→LM Studio/SD, the local-mode `imageUrl`) use plain `localhost`/`127.0.0.1` for reliable in-WSL networking, but the **browser-side** API host must follow the page host as above.

## Architecture Decision Records

Significant architecture decisions (system structure, technology choices, data flow, API design, client/server responsibility splits, persistence strategy, security boundaries) are recorded as ADRs in `docs/arch/adr-NNNN-slug.md`. Before making or revisiting a decision in one of these areas, check `docs/arch/` for prior context — the reasoning and rejected alternatives behind e.g. the client-side Firebase persistence split, the batch-generation sequential-loop design, or the SD version-compatibility degradation pattern are documented there rather than in this file. When a new significant decision is made, add a new ADR (or update Status to Superseded on an old one, never rewrite it) following the existing files' format (Context / Decision / Status / Consequences).

## Config

Server reads `server/.env`: `PORT`, `LM_STUDIO_URL`, `STABLE_DIFFUSION_URL`, `LM_STUDIO_MODEL` (empty = use LM Studio's currently-loaded model), `CORS_ORIGINS` (comma-separated allowed origins; defaults to the Vite dev origins).

Client reads `client/.env` (see `client/.env.example`): `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID`. These are the Firebase web config values from the Firebase Console. If `VITE_FIREBASE_API_KEY` is empty, Firebase is disabled entirely — no auth UI is shown and the app operates in local-only mode.

## Conventions

- Both packages are ESM (`"type": "module"`); use `import`, and the `__dirname` shim already present in `server/index.ts`.
- Client stack is intentionally lean: React 19 + Vite 8 + TypeScript, `lucide-react` for icons. Firebase is added via the `firebase` npm package (client-side SDK only — no `firebase-admin`). No router, no state library, no CSS framework — styling lives in `App.css`/`index.css`.
